# 布隆过滤器

> 超省内存的“可能存在”的判断器

利用 hash 结构的特性，实现一个快速、省内存的能够判断：我说你不在，就一定不在。我说你在，不一定在的功能

有了这个结构，就可以在一些需要快速判断：某个数据是否不在数据库里的这种请求

比如：

1. 暴力请求不存在的数据，爆破数据库
2. 垃圾邮件/黑名单过滤
3. 爬虫 URL 去重：已经爬过的URL不能再爬，判断一个 URL 是否已经爬过